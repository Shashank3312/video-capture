deepface==0.0.100
tf-keras==2.21.0
Pillow==12.3.0

# Must stay on OpenCV 4.x. The 5.0.0.93 wheel has no
# cv2.CascadeClassifier and ships no haarcascade data files, which
# breaks DeepFace's "opencv" and "ssd" backends outright, and also
# breaks any detector that doesn't return eye landmarks (yolov11,
# yolov12) because DeepFace falls back to the cascade classifier to
# find eyes for alignment.
opencv-python==4.14.0.94

# Optional in DeepFace, required for the yolo* detector backends.
ultralytics==8.4.150

# Phase 2: resolves a YouTube Live link to a playable stream URL. No
# separate ffmpeg binary is needed - the opencv-python wheel bundles
# FFmpeg, so cv2.VideoCapture opens the resulting HLS URL directly.
yt-dlp==2026.8.19

# Phase 3: speech-to-text for spoken name mentions. Pulls in PyAV,
# which decodes the audio track straight out of a video file, so
# again no ffmpeg binary is needed. Models download on first use.
faster-whisper==1.2.1
